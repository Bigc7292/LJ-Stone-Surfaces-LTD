node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz
.env
.env.*
.vercel
.env*.local

# Local Netlify folder
.netlify

# Validations
dev_error.log
# Ignore heavy build artifacts
*.zip
deploy-package/
dist/
site-deploy/

# Ignore the heavy AI training data (Keep the folder locally, but don't upload to GitHub)
fine_tuning_data/
fine_tuning_data.zip